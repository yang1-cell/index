生成图案组：
    根据卡片总数 tileList.length，计算需要生成的图案组数 totalGroups。
    每组生成3个相同图案，循环使用 symbols 数组中的图案。
打乱图案顺序：
    使用 sort() 结合 Math.random() 随机打乱 patternList（但非均匀随机）。
分配图案到卡片：
    遍历所有卡片，将打乱后的图案依次赋值给每个卡片的文本内容和数据属性。